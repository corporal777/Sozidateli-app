package com.example.ui.views.toolbar.widget

interface ITransparentDelegate {

    fun updateTop(top: Float)

    fun setMaxOffset(offset: Float)

    fun setMidOffset(offset: Float)

    fun setColorToBackGround(backGroundColor: Int)
}